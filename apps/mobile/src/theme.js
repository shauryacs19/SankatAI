// Design tokens for the mobile app — sourced from the shared package.
// `colors` is the LIGHT palette default (used by any not-yet-themed style or as
// a fallback). Themed screens read the active palette from useTheme()/useThemedStyles.
export { radius, sevColor, lightColors, darkColors, palettes } from '../../../packages/shared/theme'
import { lightColors } from '../../../packages/shared/theme'

export const colors = lightColors
