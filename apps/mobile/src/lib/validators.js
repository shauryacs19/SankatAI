// Re-exports the shared, framework-agnostic validators (see packages/shared).
// Kept as a thin shim so existing imports ('../lib/validators') stay unchanged.
export * from '../../../../packages/shared/validation'
